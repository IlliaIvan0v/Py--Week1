
1. Значение sep по ум. - sep по умолчанию рвно пробел.

2. Значение end по ум. - end по умолчанию равно \n

3. Что делает специальный символ \ ?
Специальные символы \ означает то что введеное после него символы не будут иметь значения.

4. Что делает специальный символ \n ?
Специальный символ \n перекидывает текст после него на новую строку.

5. Что делает специальный символ \t ?
Специальный символ \t делает табуляцию.

6. Что делает математический оператор % ?
Математический оператор % высчитавает остаток чисел.

7. Что делает математический оператор // ?
Математический оператор //
Высчитывает целочисленное число.

8. Что делает математический оператор ** ?
Математический оператор ** возводит числа в степень

9. Что возвращает abs(-2) ?
  Код выведит число 2.

10. Что возвращает pow(2, 4) ?
Код выведит число 16.

11. Что возвратит round(5 / 2) ?
Код выведит число  3.

# второй аргумент в функции round означает до скольки символов после запятой будет выполняться округление,
 например round(10 / 3, 2) вернёт 3.33

12. Зачем нужна директива del ?
Директива del используются для того что бы удалить переменные, элементы в списке и т. д.

11. Создайте переменную "age" и присвойте ей значение вашего возраста. Выведите ее значение.
 Затем используйте директиву "del" для удаления этой переменной, и попробуйте вывести значение снова.
  Что произошло?
  пройзодет ошибка так как переменная age не будет найдена так как прежде была выполнена команда del
  которая удалила переменую age. 